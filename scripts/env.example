# Copy to scripts/.env (gitignored) or export these in your shell.
# Do not commit real values. Docker Compose and Make read them from the environment.
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
INGRESS_PORT=3000
